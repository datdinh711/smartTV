import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smarttv',
  appName: 'smartTV',
  webDir: 'dist/smart-tv/browser',
};

export default config;
